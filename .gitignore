.env
*.env
!.env.example

.codex
.venv/
models/

__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

*.log
*.tmp
*.bak
*.bak-*
tts_*.mp3
data/*.json

.DS_Store
Thumbs.db

# Voice samples and clone previews are operator-local files (may
# contain personal recordings); never commit them.
scripts/voice_samples/*.mp3
scripts/voice_samples/*.wav
scripts/voice_samples/*.m4a
scripts/voice_samples/_preview_*

# Local Discord chat exports / metrics (personal data; never commit)
exports/

# Runtime state and local diagnostics (cache, voice registry, wav dumps)
data/
.env.bak*
supertonic_*.md

# Host-specific compose tweaks stay local
docker-compose.override.yml
