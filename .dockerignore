# Allowlist: the image copies only requirements.txt, bot.py and ttsbot/
# (see Dockerfile), so exclude everything else from the build context.
*
!requirements.txt
!bot.py
!ttsbot
**/__pycache__
**/*.py[cod]
